apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grpc-gateway-cert
spec:
  commonName: grpc.apps.example.com
  dnsNames:
  - grpc.apps.example.com
  isCA: false
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: example-cluster-issuer
  privateKey:
    algorithm: ECDSA
    size: 256
  secretName: gateway-secret
  subject:
    countries:
    - US
    organizationalUnits:
    - Example
    organizations:
    - Example
