apiVersion: telemetry.istio.io/v1
kind: Telemetry
metadata:
  name: tenanta
spec:
  accessLogging:
    - disabled: false
      providers:
        - name: tenanta
  metrics:
    - overrides:
        - disabled: false
      providers:
        - name: prometheus
  tracing:
    - providers:
        - name: tenanta
      randomSamplingPercentage: 100
